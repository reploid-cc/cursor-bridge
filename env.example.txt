# Server configuration
NODE_ENV=production
API_PORT=1000
LOG_LEVEL=info

# Browser configuration
BROWSER_TYPE=chrome

# n8n connection
N8N_HOST=host.docker.internal
N8N_PORT=5678

# Cursor Browser Automation - Environment Variables
# Copy file này thành .env và điền thông tin của bạn

# Thông tin đăng nhập Cursor
CURSOR_USERNAME=your_email@example.com
CURSOR_PASSWORD=your_password

# Cấu hình Browser
BROWSER_HEADLESS=true
BROWSER_SLOW_MO=50
BROWSER_TIMEOUT=30000
BROWSER_VIEWPORT_WIDTH=1280
BROWSER_VIEWPORT_HEIGHT=800

# Đường dẫn tới Chrome/Chromium (để trống để sử dụng mặc định)
PLAYWRIGHT_BROWSERS_PATH= 